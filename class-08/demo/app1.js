function yodaGreeting(){
    let userAnswer = prompt('Please enter a yes or no!')
    if (userAnswer != 'yes' && userAnswer != 'no'){
        alert('You entered incorrectly')
    }
}

// yodaGreeting();